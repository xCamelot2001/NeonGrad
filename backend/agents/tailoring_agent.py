"""
Tailoring Agent — Phase 3 (LangGraph multi-agent with judge loop).
Stub so imports resolve.
"""
from typing import AsyncIterator


async def run_tailoring(application_id: str, user_id: str) -> AsyncIterator[str]:
    raise NotImplementedError("Tailoring agent is implemented in Phase 3.")
