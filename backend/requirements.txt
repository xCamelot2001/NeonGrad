# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# Supabase / DB
supabase==2.4.6
postgrest==0.16.6

# CV parsing
pymupdf==1.25.5
python-docx==1.1.2

# Groq API (LLM)
groq==0.9.0

# LangGraph (Phase 2+)
# Pinned <1.0: langchain-core 1.x pulls in langchain-protocol which uses
# TypedDict(extra_items=...) — a Python 3.13+ feature. The 0.x series is
# stable and covers everything we need through Phase 5.
# DO NOT add 'langchain' (the full package) — it's not used here and old
# installed versions cause AttributeError: module 'langchain' has no attribute 'debug'.
langgraph>=0.2.0,<1.0
langchain-core>=0.2.0,<1.0

# Relevance scorer — sentence-transformer inference (Phase 2)
sentence-transformers>=3.0.0
torch>=2.2.0
huggingface_hub>=0.23.0

# HTTP client
httpx==0.27.0

# Scheduling (Phase 2)
apscheduler==3.10.4

# Env management
python-dotenv==1.0.1

# Phase 3 — Tailoring agent
tavily-python==0.3.9        # company research via Tavily search API
reportlab==4.2.5            # PDF generation for tailored CV + cover letter

# Utilities
pydantic==2.11.4
